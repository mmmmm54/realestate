/* ---------------------------------------------------------------------------
   Configuration locale — copiez ce fichier en « config.js » et remplissez-le.
   config.js est exclu du dépôt : aucun jeton ne part sur GitHub.

   Sans jeton, le site fonctionne quand même : la carte satellite est remplacée
   par la carte vectorielle générée à partir d'OpenStreetMap. Rien ne casse.
   --------------------------------------------------------------------------- */
window.MAALEM_CONFIG = {

  /* Jeton PUBLIC Mapbox — il commence par « pk. ».
     JAMAIS un jeton secret « sk. ».
     Restreignez-le à votre domaine : Account > Tokens > URL restrictions.
     Sans restriction, n'importe qui peut le copier et consommer votre quota. */
  mapboxToken: ''

};
